import{_ as e,o,c as s,V as l}from"./chunks/framework.ced88878.js";const y=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/reference/Module Resolution.md","lastUpdated":1682670826000}'),t={name:"en/reference/Module Resolution.md"},a=l(`<blockquote><p>This section assumes some basic knowledge about modules. Please see the <a href="/tsdoc-vitepress/docs/handbook/modules.html">Modules</a> documentation for more information.</p></blockquote><p><em>Module resolution</em> is the process the compiler uses to figure out what an import refers to. Consider an import statement like <code>import { a } from &quot;moduleA&quot;</code>; in order to check any use of <code>a</code>, the compiler needs to know exactly what it represents, and will need to check its definition <code>moduleA</code>.</p><p>At this point, the compiler will ask &quot;what&#39;s the shape of <code>moduleA</code>?&quot; While this sounds straightforward, <code>moduleA</code> could be defined in one of your own <code>.ts</code>/<code>.tsx</code> files, or in a <code>.d.ts</code> that your code depends on.</p><p>First, the compiler will try to locate a file that represents the imported module. To do so the compiler follows one of two different strategies: <a href="#classic">Classic</a> or <a href="#node">Node</a>. These strategies tell the compiler <em>where</em> to look for <code>moduleA</code>.</p><p>If that didn&#39;t work and if the module name is non-relative (and in the case of <code>&quot;moduleA&quot;</code>, it is), then the compiler will attempt to locate an <a href="/tsdoc-vitepress/docs/handbook/modules.html#ambient-modules">ambient module declaration</a>. We&#39;ll cover non-relative imports next.</p><p>Finally, if the compiler could not resolve the module, it will log an error. In this case, the error would be something like <code>error TS2307: Cannot find module &#39;moduleA&#39;.</code></p><h2 id="relative-vs-non-relative-module-imports" tabindex="-1">Relative vs. Non-relative module imports <a class="header-anchor" href="#relative-vs-non-relative-module-imports" aria-label="Permalink to &quot;Relative vs. Non-relative module imports&quot;">​</a></h2><p>Module imports are resolved differently based on whether the module reference is relative or non-relative.</p><p>A <em>relative import</em> is one that starts with <code>/</code>, <code>./</code> or <code>../</code>. Some examples include:</p><ul><li><code>import Entry from &quot;./components/Entry&quot;;</code></li><li><code>import { DefaultHeaders } from &quot;../constants/http&quot;;</code></li><li><code>import &quot;/mod&quot;;</code></li></ul><p>Any other import is considered <strong>non-relative</strong>. Some examples include:</p><ul><li><code>import * as $ from &quot;jquery&quot;;</code></li><li><code>import { Component } from &quot;@angular/core&quot;;</code></li></ul><p>A relative import is resolved relative to the importing file and <em>cannot</em> resolve to an ambient module declaration. You should use relative imports for your own modules that are guaranteed to maintain their relative location at runtime.</p><p>A non-relative import can be resolved relative to <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a>, or through path mapping, which we&#39;ll cover below. They can also resolve to <a href="/tsdoc-vitepress/handbooks/handbook-v2/Modules#ambient-modules">ambient module declarations</a>. Use non-relative paths when importing any of your external dependencies.</p><h2 id="module-resolution-strategies" tabindex="-1">Module Resolution Strategies <a class="header-anchor" href="#module-resolution-strategies" aria-label="Permalink to &quot;Module Resolution Strategies&quot;">​</a></h2><p>There are two possible module resolution strategies: <a href="#node">Node</a> and <a href="#classic">Classic</a>. You can use the <a href="/tsdoc-vitepress/tsconfig#moduleResolution"><code>moduleResolution</code></a> option to specify the module resolution strategy. If not specified, the default is <a href="#node">Node</a> for <code>--module commonjs</code>, and <a href="#classic">Classic</a> otherwise (including when <a href="/tsdoc-vitepress/tsconfig#module"><code>module</code></a> is set to <code>amd</code>, <code>system</code>, <code>umd</code>, <code>es2015</code>, <code>esnext</code>, etc.).</p><blockquote><p>Note: <code>node</code> module resolution is the most-commonly used in the TypeScript community and is recommended for most projects. If you are having resolution problems with <code>import</code>s and <code>export</code>s in TypeScript, try setting <code>moduleResolution: &quot;node&quot;</code> to see if it fixes the issue.</p></blockquote><h3 id="classic" tabindex="-1">Classic <a class="header-anchor" href="#classic" aria-label="Permalink to &quot;Classic&quot;">​</a></h3><p>This used to be TypeScript&#39;s default resolution strategy. Nowadays, this strategy is mainly present for backward compatibility.</p><p>A relative import will be resolved relative to the importing file. So <code>import { b } from &quot;./moduleB&quot;</code> in source file <code>/root/src/folder/A.ts</code> would result in the following lookups:</p><ol><li><code>/root/src/folder/moduleB.ts</code></li><li><code>/root/src/folder/moduleB.d.ts</code></li></ol><p>For non-relative module imports, however, the compiler walks up the directory tree starting with the directory containing the importing file, trying to locate a matching definition file.</p><p>For example:</p><p>A non-relative import to <code>moduleB</code> such as <code>import { b } from &quot;moduleB&quot;</code>, in a source file <code>/root/src/folder/A.ts</code>, would result in attempting the following locations for locating <code>&quot;moduleB&quot;</code>:</p><ol><li><code>/root/src/folder/moduleB.ts</code></li><li><code>/root/src/folder/moduleB.d.ts</code></li><li><code>/root/src/moduleB.ts</code></li><li><code>/root/src/moduleB.d.ts</code></li><li><code>/root/moduleB.ts</code></li><li><code>/root/moduleB.d.ts</code></li><li><code>/moduleB.ts</code></li><li><code>/moduleB.d.ts</code></li></ol><h3 id="node" tabindex="-1">Node <a class="header-anchor" href="#node" aria-label="Permalink to &quot;Node&quot;">​</a></h3><p>This resolution strategy attempts to mimic the <a href="https://nodejs.org/" target="_blank" rel="noreferrer">Node.js</a> module resolution mechanism at runtime. The full Node.js resolution algorithm is outlined in <a href="https://nodejs.org/api/modules.html#modules_all_together" target="_blank" rel="noreferrer">Node.js module documentation</a>.</p><h4 id="how-node-js-resolves-modules" tabindex="-1">How Node.js resolves modules <a class="header-anchor" href="#how-node-js-resolves-modules" aria-label="Permalink to &quot;How Node.js resolves modules&quot;">​</a></h4><p>To understand what steps the TS compiler will follow, it is important to shed some light on Node.js modules. Traditionally, imports in Node.js are performed by calling a function named <code>require</code>. The behavior Node.js takes will differ depending on if <code>require</code> is given a relative path or a non-relative path.</p><p>Relative paths are fairly straightforward. As an example, let&#39;s consider a file located at <code>/root/src/moduleA.js</code>, which contains the import <code>var x = require(&quot;./moduleB&quot;);</code> Node.js resolves that import in the following order:</p><ol><li><p>Ask the file named <code>/root/src/moduleB.js</code>, if it exists.</p></li><li><p>Ask the folder <code>/root/src/moduleB</code> if it contains a file named <code>package.json</code> that specifies a <code>&quot;main&quot;</code> module. In our example, if Node.js found the file <code>/root/src/moduleB/package.json</code> containing <code>{ &quot;main&quot;: &quot;lib/mainModule.js&quot; }</code>, then Node.js will refer to <code>/root/src/moduleB/lib/mainModule.js</code>.</p></li><li><p>Ask the folder <code>/root/src/moduleB</code> if it contains a file named <code>index.js</code>. That file is implicitly considered that folder&#39;s &quot;main&quot; module.</p></li></ol><p>You can read more about this in Node.js documentation on <a href="https://nodejs.org/api/modules.html#modules_file_modules" target="_blank" rel="noreferrer">file modules</a> and <a href="https://nodejs.org/api/modules.html#modules_folders_as_modules" target="_blank" rel="noreferrer">folder modules</a>.</p><p>However, resolution for a <a href="#relative-vs-non-relative-module-imports">non-relative module name</a> is performed differently. Node will look for your modules in special folders named <code>node_modules</code>. A <code>node_modules</code> folder can be on the same level as the current file, or higher up in the directory chain. Node will walk up the directory chain, looking through each <code>node_modules</code> until it finds the module you tried to load.</p><p>Following up our example above, consider if <code>/root/src/moduleA.js</code> instead used a non-relative path and had the import <code>var x = require(&quot;moduleB&quot;);</code>. Node would then try to resolve <code>moduleB</code> to each of the locations until one worked.</p><ol><li><code>/root/src/node_modules/moduleB.js</code></li><li><code>/root/src/node_modules/moduleB/package.json</code> (if it specifies a <code>&quot;main&quot;</code> property)</li><li><code>/root/src/node_modules/moduleB/index.js</code><br><br></li><li><code>/root/node_modules/moduleB.js</code></li><li><code>/root/node_modules/moduleB/package.json</code> (if it specifies a <code>&quot;main&quot;</code> property)</li><li><code>/root/node_modules/moduleB/index.js</code><br><br></li><li><code>/node_modules/moduleB.js</code></li><li><code>/node_modules/moduleB/package.json</code> (if it specifies a <code>&quot;main&quot;</code> property)</li><li><code>/node_modules/moduleB/index.js</code></li></ol><p>Notice that Node.js jumped up a directory in steps (4) and (7).</p><p>You can read more about the process in Node.js documentation on <a href="https://nodejs.org/api/modules.html#modules_loading_from_node_modules_folders" target="_blank" rel="noreferrer">loading modules from <code>node_modules</code></a>.</p><h4 id="how-typescript-resolves-modules" tabindex="-1">How TypeScript resolves modules <a class="header-anchor" href="#how-typescript-resolves-modules" aria-label="Permalink to &quot;How TypeScript resolves modules&quot;">​</a></h4><p>TypeScript will mimic the Node.js run-time resolution strategy in order to locate definition files for modules at compile-time. To accomplish this, TypeScript overlays the TypeScript source file extensions (<code>.ts</code>, <code>.tsx</code>, and <code>.d.ts</code>) over Node&#39;s resolution logic. TypeScript will also use a field in <code>package.json</code> named <code>types</code> to mirror the purpose of <code>&quot;main&quot;</code> - the compiler will use it to find the &quot;main&quot; definition file to consult.</p><p>For example, an import statement like <code>import { b } from &quot;./moduleB&quot;</code> in <code>/root/src/moduleA.ts</code> would result in attempting the following locations for locating <code>&quot;./moduleB&quot;</code>:</p><ol><li><code>/root/src/moduleB.ts</code></li><li><code>/root/src/moduleB.tsx</code></li><li><code>/root/src/moduleB.d.ts</code></li><li><code>/root/src/moduleB/package.json</code> (if it specifies a <code>types</code> property)</li><li><code>/root/src/moduleB/index.ts</code></li><li><code>/root/src/moduleB/index.tsx</code></li><li><code>/root/src/moduleB/index.d.ts</code></li></ol><p>Recall that Node.js looked for a file named <code>moduleB.js</code>, then an applicable <code>package.json</code>, and then for an <code>index.js</code>.</p><p>Similarly, a non-relative import will follow the Node.js resolution logic, first looking up a file, then looking up an applicable folder. So <code>import { b } from &quot;moduleB&quot;</code> in source file <code>/root/src/moduleA.ts</code> would result in the following lookups:</p><ol><li><code>/root/src/node_modules/moduleB.ts</code></li><li><code>/root/src/node_modules/moduleB.tsx</code></li><li><code>/root/src/node_modules/moduleB.d.ts</code></li><li><code>/root/src/node_modules/moduleB/package.json</code> (if it specifies a <code>types</code> property)</li><li><code>/root/src/node_modules/@types/moduleB.d.ts</code></li><li><code>/root/src/node_modules/moduleB/index.ts</code></li><li><code>/root/src/node_modules/moduleB/index.tsx</code></li><li><code>/root/src/node_modules/moduleB/index.d.ts</code><br><br></li><li><code>/root/node_modules/moduleB.ts</code></li><li><code>/root/node_modules/moduleB.tsx</code></li><li><code>/root/node_modules/moduleB.d.ts</code></li><li><code>/root/node_modules/moduleB/package.json</code> (if it specifies a <code>types</code> property)</li><li><code>/root/node_modules/@types/moduleB.d.ts</code></li><li><code>/root/node_modules/moduleB/index.ts</code></li><li><code>/root/node_modules/moduleB/index.tsx</code></li><li><code>/root/node_modules/moduleB/index.d.ts</code><br><br></li><li><code>/node_modules/moduleB.ts</code></li><li><code>/node_modules/moduleB.tsx</code></li><li><code>/node_modules/moduleB.d.ts</code></li><li><code>/node_modules/moduleB/package.json</code> (if it specifies a <code>types</code> property)</li><li><code>/node_modules/@types/moduleB.d.ts</code></li><li><code>/node_modules/moduleB/index.ts</code></li><li><code>/node_modules/moduleB/index.tsx</code></li><li><code>/node_modules/moduleB/index.d.ts</code></li></ol><p>Don&#39;t be intimidated by the number of steps here - TypeScript is still only jumping up directories twice at steps (9) and (17). This is really no more complex than what Node.js itself is doing.</p><h2 id="additional-module-resolution-flags" tabindex="-1">Additional module resolution flags <a class="header-anchor" href="#additional-module-resolution-flags" aria-label="Permalink to &quot;Additional module resolution flags&quot;">​</a></h2><p>A project source layout sometimes does not match that of the output. Usually a set of build steps result in generating the final output. These include compiling <code>.ts</code> files into <code>.js</code>, and copying dependencies from different source locations to a single output location. The net result is that modules at runtime may have different names than the source files containing their definitions. Or module paths in the final output may not match their corresponding source file paths at compile time.</p><p>The TypeScript compiler has a set of additional flags to <em>inform</em> the compiler of transformations that are expected to happen to the sources to generate the final output.</p><p>It is important to note that the compiler will <em>not</em> perform any of these transformations; it just uses these pieces of information to guide the process of resolving a module import to its definition file.</p><h3 id="base-url" tabindex="-1">Base URL <a class="header-anchor" href="#base-url" aria-label="Permalink to &quot;Base URL&quot;">​</a></h3><p>Using a <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a> is a common practice in applications using AMD module loaders where modules are &quot;deployed&quot; to a single folder at run-time. The sources of these modules can live in different directories, but a build script will put them all together.</p><p>Setting <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a> informs the compiler where to find modules. All module imports with non-relative names are assumed to be relative to the <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a>.</p><p>Value of <em>baseUrl</em> is determined as either:</p><ul><li>value of <em>baseUrl</em> command line argument (if given path is relative, it is computed based on current directory)</li><li>value of <em>baseUrl</em> property in &#39;tsconfig.json&#39; (if given path is relative, it is computed based on the location of &#39;tsconfig.json&#39;)</li></ul><p>Note that relative module imports are not impacted by setting the baseUrl, as they are always resolved relative to their importing files.</p><p>You can find more documentation on baseUrl in <a href="http://requirejs.org/docs/api.html#config-baseUrl" target="_blank" rel="noreferrer">RequireJS</a> and <a href="https://github.com/systemjs/systemjs/blob/main/docs/api.md" target="_blank" rel="noreferrer">SystemJS</a> documentation.</p><h3 id="path-mapping" tabindex="-1">Path mapping <a class="header-anchor" href="#path-mapping" aria-label="Permalink to &quot;Path mapping&quot;">​</a></h3><p>Sometimes modules are not directly located under <em>baseUrl</em>. For instance, an import to a module <code>&quot;jquery&quot;</code> would be translated at runtime to <code>&quot;node_modules/jquery/dist/jquery.slim.min.js&quot;</code>. Loaders use a mapping configuration to map module names to files at run-time, see <a href="http://requirejs.org/docs/api.html#config-paths" target="_blank" rel="noreferrer">RequireJs documentation</a> and <a href="https://github.com/systemjs/systemjs/blob/main/docs/import-maps.md" target="_blank" rel="noreferrer">SystemJS documentation</a>.</p><p>The TypeScript compiler supports the declaration of such mappings using <a href="/tsdoc-vitepress/tsconfig#paths"><code>paths</code></a> property in <code>tsconfig.json</code> files. Here is an example for how to specify the <a href="/tsdoc-vitepress/tsconfig#paths"><code>paths</code></a> property for <code>jquery</code>.</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">{</span></span>
<span class="line"><span style="color:#F6F6F4;">  </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">compilerOptions</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">baseUrl</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">.</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#7B7F8B;">// This must be specified if &quot;paths&quot; is.</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">paths</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">      </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">jquery</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> [</span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">node_modules/jquery/dist/jquery</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">] </span><span style="color:#7B7F8B;">// This mapping is relative to &quot;baseUrl&quot;</span></span>
<span class="line"><span style="color:#F6F6F4;">    }</span></span>
<span class="line"><span style="color:#F6F6F4;">  }</span></span>
<span class="line"><span style="color:#F6F6F4;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">{</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#005CC5;">&quot;compilerOptions&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;baseUrl&quot;</span><span style="color:#24292E;">: </span><span style="color:#032F62;">&quot;.&quot;</span><span style="color:#24292E;">, </span><span style="color:#6A737D;">// This must be specified if &quot;paths&quot; is.</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;paths&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">      </span><span style="color:#005CC5;">&quot;jquery&quot;</span><span style="color:#24292E;">: [</span><span style="color:#032F62;">&quot;node_modules/jquery/dist/jquery&quot;</span><span style="color:#24292E;">] </span><span style="color:#6A737D;">// This mapping is relative to &quot;baseUrl&quot;</span></span>
<span class="line"><span style="color:#24292E;">    }</span></span>
<span class="line"><span style="color:#24292E;">  }</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>Please notice that <a href="/tsdoc-vitepress/tsconfig#paths"><code>paths</code></a> are resolved relative to <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a>. When setting <a href="/tsdoc-vitepress/tsconfig#baseUrl"><code>baseUrl</code></a> to a value other than <code>&quot;.&quot;</code>, i.e. the directory of <code>tsconfig.json</code>, the mappings must be changed accordingly. Say, you set <code>&quot;baseUrl&quot;: &quot;./src&quot;</code> in the above example, then jquery should be mapped to <code>&quot;../node_modules/jquery/dist/jquery&quot;</code>.</p><p>Using <a href="/tsdoc-vitepress/tsconfig#paths"><code>paths</code></a> also allows for more sophisticated mappings including multiple fall back locations. Consider a project configuration where only some modules are available in one location, and the rest are in another. A build step would put them all together in one place. The project layout may look like:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">projectRoot</span></span>
<span class="line"><span style="color:#f6f6f4;">├── folder1</span></span>
<span class="line"><span style="color:#f6f6f4;">│   ├── file1.ts (imports &#39;folder1/file2&#39; and &#39;folder2/file3&#39;)</span></span>
<span class="line"><span style="color:#f6f6f4;">│   └── file2.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">├── generated</span></span>
<span class="line"><span style="color:#f6f6f4;">│   ├── folder1</span></span>
<span class="line"><span style="color:#f6f6f4;">│   └── folder2</span></span>
<span class="line"><span style="color:#f6f6f4;">│       └── file3.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">└── tsconfig.json</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">projectRoot</span></span>
<span class="line"><span style="color:#24292e;">├── folder1</span></span>
<span class="line"><span style="color:#24292e;">│   ├── file1.ts (imports &#39;folder1/file2&#39; and &#39;folder2/file3&#39;)</span></span>
<span class="line"><span style="color:#24292e;">│   └── file2.ts</span></span>
<span class="line"><span style="color:#24292e;">├── generated</span></span>
<span class="line"><span style="color:#24292e;">│   ├── folder1</span></span>
<span class="line"><span style="color:#24292e;">│   └── folder2</span></span>
<span class="line"><span style="color:#24292e;">│       └── file3.ts</span></span>
<span class="line"><span style="color:#24292e;">└── tsconfig.json</span></span></code></pre></div><p>The corresponding <code>tsconfig.json</code> would look like:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">{</span></span>
<span class="line"><span style="color:#F6F6F4;">  </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">compilerOptions</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">baseUrl</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">.</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">,</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">paths</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">      </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">*</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> [</span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">*</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">generated/*</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">]</span></span>
<span class="line"><span style="color:#F6F6F4;">    }</span></span>
<span class="line"><span style="color:#F6F6F4;">  }</span></span>
<span class="line"><span style="color:#F6F6F4;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">{</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#005CC5;">&quot;compilerOptions&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;baseUrl&quot;</span><span style="color:#24292E;">: </span><span style="color:#032F62;">&quot;.&quot;</span><span style="color:#24292E;">,</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;paths&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">      </span><span style="color:#005CC5;">&quot;*&quot;</span><span style="color:#24292E;">: [</span><span style="color:#032F62;">&quot;*&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;generated/*&quot;</span><span style="color:#24292E;">]</span></span>
<span class="line"><span style="color:#24292E;">    }</span></span>
<span class="line"><span style="color:#24292E;">  }</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>This tells the compiler for any module import that matches the pattern <code>&quot;*&quot;</code> (i.e. all values), to look in two locations:</p><ol><li><code>&quot;*&quot;</code>: meaning the same name unchanged, so map <code>&lt;moduleName&gt;</code> =&gt; <code>&lt;baseUrl&gt;/&lt;moduleName&gt;</code></li><li><code>&quot;generated/*&quot;</code> meaning the module name with an appended prefix &quot;generated&quot;, so map <code>&lt;moduleName&gt;</code> =&gt; <code>&lt;baseUrl&gt;/generated/&lt;moduleName&gt;</code></li></ol><p>Following this logic, the compiler will attempt to resolve the two imports as such:</p><p>import &#39;folder1/file2&#39;:</p><ol><li>pattern &#39;*&#39; is matched and wildcard captures the whole module name</li><li>try first substitution in the list: &#39;*&#39; -&gt; <code>folder1/file2</code></li><li>result of substitution is non-relative name - combine it with <em>baseUrl</em> -&gt; <code>projectRoot/folder1/file2.ts</code>.</li><li>File exists. Done.</li></ol><p>import &#39;folder2/file3&#39;:</p><ol><li>pattern &#39;*&#39; is matched and wildcard captures the whole module name</li><li>try first substitution in the list: &#39;*&#39; -&gt; <code>folder2/file3</code></li><li>result of substitution is non-relative name - combine it with <em>baseUrl</em> -&gt; <code>projectRoot/folder2/file3.ts</code>.</li><li>File does not exist, move to the second substitution</li><li>second substitution &#39;generated/*&#39; -&gt; <code>generated/folder2/file3</code></li><li>result of substitution is non-relative name - combine it with <em>baseUrl</em> -&gt; <code>projectRoot/generated/folder2/file3.ts</code>.</li><li>File exists. Done.</li></ol><h3 id="virtual-directories-with-rootdirs" tabindex="-1">Virtual Directories with <code>rootDirs</code> <a class="header-anchor" href="#virtual-directories-with-rootdirs" aria-label="Permalink to &quot;Virtual Directories with \`rootDirs\`&quot;">​</a></h3><p>Sometimes the project sources from multiple directories at compile time are all combined to generate a single output directory. This can be viewed as a set of source directories create a &quot;virtual&quot; directory.</p><p>Using <code>rootDirs</code>, you can inform the compiler of the <em>roots</em> making up this &quot;virtual&quot; directory; and thus the compiler can resolve relative modules imports within these &quot;virtual&quot; directories <em>as if</em> they were merged together in one directory.</p><p>For example consider this project structure:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">src</span></span>
<span class="line"><span style="color:#f6f6f4;"> └── views</span></span>
<span class="line"><span style="color:#f6f6f4;">     └── view1.ts (imports &#39;./template1&#39;)</span></span>
<span class="line"><span style="color:#f6f6f4;">     └── view2.ts</span></span>
<span class="line"><span style="color:#f6f6f4;"></span></span>
<span class="line"><span style="color:#f6f6f4;"> generated</span></span>
<span class="line"><span style="color:#f6f6f4;"> └── templates</span></span>
<span class="line"><span style="color:#f6f6f4;">         └── views</span></span>
<span class="line"><span style="color:#f6f6f4;">             └── template1.ts (imports &#39;./view2&#39;)</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">src</span></span>
<span class="line"><span style="color:#24292e;"> └── views</span></span>
<span class="line"><span style="color:#24292e;">     └── view1.ts (imports &#39;./template1&#39;)</span></span>
<span class="line"><span style="color:#24292e;">     └── view2.ts</span></span>
<span class="line"><span style="color:#24292e;"></span></span>
<span class="line"><span style="color:#24292e;"> generated</span></span>
<span class="line"><span style="color:#24292e;"> └── templates</span></span>
<span class="line"><span style="color:#24292e;">         └── views</span></span>
<span class="line"><span style="color:#24292e;">             └── template1.ts (imports &#39;./view2&#39;)</span></span></code></pre></div><p>Files in <code>src/views</code> are user code for some UI controls. Files in <code>generated/templates</code> are UI template binding code auto-generated by a template generator as part of the build. A build step will copy the files in <code>/src/views</code> and <code>/generated/templates/views</code> to the same directory in the output. At run-time, a view can expect its template to exist next to it, and thus should import it using a relative name as <code>&quot;./template&quot;</code>.</p><p>To specify this relationship to the compiler, use <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a>. <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a> specify a list of <em>roots</em> whose contents are expected to merge at run-time. So following our example, the <code>tsconfig.json</code> file should look like:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">{</span></span>
<span class="line"><span style="color:#F6F6F4;">  </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">compilerOptions</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">rootDirs</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> [</span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">src/views</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">generated/templates/views</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">]</span></span>
<span class="line"><span style="color:#F6F6F4;">  }</span></span>
<span class="line"><span style="color:#F6F6F4;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">{</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#005CC5;">&quot;compilerOptions&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;rootDirs&quot;</span><span style="color:#24292E;">: [</span><span style="color:#032F62;">&quot;src/views&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;generated/templates/views&quot;</span><span style="color:#24292E;">]</span></span>
<span class="line"><span style="color:#24292E;">  }</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>Every time the compiler sees a relative module import in a subfolder of one of the <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a>, it will attempt to look for this import in each of the entries of <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a>.</p><p>The flexibility of <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a> is not limited to specifying a list of physical source directories that are logically merged. The supplied array may include any number of ad hoc, arbitrary directory names, regardless of whether they exist or not. This allows the compiler to capture sophisticated bundling and runtime features such as conditional inclusion and project specific loader plugins in a type safe way.</p><p>Consider an internationalization scenario where a build tool automatically generates locale specific bundles by interpolating a special path token, say <code>#{locale}</code>, as part of a relative module path such as <code>./#{locale}/messages</code>. In this hypothetical setup the tool enumerates supported locales, mapping the abstracted path into <code>./zh/messages</code>, <code>./de/messages</code>, and so forth.</p><p>Assume that each of these modules exports an array of strings. For example <code>./zh/messages</code> might contain:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F286C4;">export</span><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">default</span><span style="color:#F6F6F4;"> [</span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">您好吗</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">很高兴认识你</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">];</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">export</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">default</span><span style="color:#24292E;"> [</span><span style="color:#032F62;">&quot;您好吗&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;很高兴认识你&quot;</span><span style="color:#24292E;">];</span></span></code></pre></div><p>By leveraging <a href="/tsdoc-vitepress/tsconfig#rootDirs"><code>rootDirs</code></a> we can inform the compiler of this mapping and thereby allow it to safely resolve <code>./#{locale}/messages</code>, even though the directory will never exist. For example, with the following <code>tsconfig.json</code>:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">{</span></span>
<span class="line"><span style="color:#F6F6F4;">  </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">compilerOptions</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> {</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#97E2F2;">&quot;</span><span style="color:#97E1F1;">rootDirs</span><span style="color:#97E2F2;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> [</span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">src/zh</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">src/de</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">src/#{locale}</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">]</span></span>
<span class="line"><span style="color:#F6F6F4;">  }</span></span>
<span class="line"><span style="color:#F6F6F4;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">{</span></span>
<span class="line"><span style="color:#24292E;">  </span><span style="color:#005CC5;">&quot;compilerOptions&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#005CC5;">&quot;rootDirs&quot;</span><span style="color:#24292E;">: [</span><span style="color:#032F62;">&quot;src/zh&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;src/de&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;src/#{locale}&quot;</span><span style="color:#24292E;">]</span></span>
<span class="line"><span style="color:#24292E;">  }</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>The compiler will now resolve <code>import messages from &#39;./#{locale}/messages&#39;</code> to <code>import messages from &#39;./zh/messages&#39;</code> for tooling purposes, allowing development in a locale agnostic manner without compromising design time support.</p><h2 id="tracing-module-resolution" tabindex="-1">Tracing module resolution <a class="header-anchor" href="#tracing-module-resolution" aria-label="Permalink to &quot;Tracing module resolution&quot;">​</a></h2><p>As discussed earlier, the compiler can visit files outside the current folder when resolving a module. This can be hard when diagnosing why a module is not resolved, or is resolved to an incorrect definition. Enabling the compiler module resolution tracing using <a href="/tsdoc-vitepress/tsconfig#traceResolution"><code>traceResolution</code></a> provides insight in what happened during the module resolution process.</p><p>Let&#39;s say we have a sample application that uses the <code>typescript</code> module. <code>app.ts</code> has an import like <code>import * as ts from &quot;typescript&quot;</code>.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">│   tsconfig.json</span></span>
<span class="line"><span style="color:#f6f6f4;">├───node_modules</span></span>
<span class="line"><span style="color:#f6f6f4;">│   └───typescript</span></span>
<span class="line"><span style="color:#f6f6f4;">│       └───lib</span></span>
<span class="line"><span style="color:#f6f6f4;">│               typescript.d.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">└───src</span></span>
<span class="line"><span style="color:#f6f6f4;">        app.ts</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">│   tsconfig.json</span></span>
<span class="line"><span style="color:#24292e;">├───node_modules</span></span>
<span class="line"><span style="color:#24292e;">│   └───typescript</span></span>
<span class="line"><span style="color:#24292e;">│       └───lib</span></span>
<span class="line"><span style="color:#24292e;">│               typescript.d.ts</span></span>
<span class="line"><span style="color:#24292e;">└───src</span></span>
<span class="line"><span style="color:#24292e;">        app.ts</span></span></code></pre></div><p>Invoking the compiler with <a href="/tsdoc-vitepress/tsconfig#traceResolution"><code>traceResolution</code></a></p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">tsc </span><span style="color:#BF9EEE;">--traceResolution</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">tsc</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">--traceResolution</span></span></code></pre></div><p>Results in an output such as:</p><div class="language-txt"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">======== Resolving module &#39;typescript&#39; from &#39;src/app.ts&#39;. ========</span></span>
<span class="line"><span style="color:#f6f6f4;">Module resolution kind is not specified, using &#39;NodeJs&#39;.</span></span>
<span class="line"><span style="color:#f6f6f4;">Loading module &#39;typescript&#39; from &#39;node_modules&#39; folder.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;src/node_modules/typescript.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;src/node_modules/typescript.tsx&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;src/node_modules/typescript.d.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;src/node_modules/typescript/package.json&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;node_modules/typescript.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;node_modules/typescript.tsx&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;node_modules/typescript.d.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#f6f6f4;">Found &#39;package.json&#39; at &#39;node_modules/typescript/package.json&#39;.</span></span>
<span class="line"><span style="color:#f6f6f4;">&#39;package.json&#39; has &#39;types&#39; field &#39;./lib/typescript.d.ts&#39; that references &#39;node_modules/typescript/lib/typescript.d.ts&#39;.</span></span>
<span class="line"><span style="color:#f6f6f4;">File &#39;node_modules/typescript/lib/typescript.d.ts&#39; exist - use it as a module resolution result.</span></span>
<span class="line"><span style="color:#f6f6f4;">======== Module name &#39;typescript&#39; was successfully resolved to &#39;node_modules/typescript/lib/typescript.d.ts&#39;. ========</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">======== Resolving module &#39;typescript&#39; from &#39;src/app.ts&#39;. ========</span></span>
<span class="line"><span style="color:#24292e;">Module resolution kind is not specified, using &#39;NodeJs&#39;.</span></span>
<span class="line"><span style="color:#24292e;">Loading module &#39;typescript&#39; from &#39;node_modules&#39; folder.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;src/node_modules/typescript.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;src/node_modules/typescript.tsx&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;src/node_modules/typescript.d.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;src/node_modules/typescript/package.json&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;node_modules/typescript.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;node_modules/typescript.tsx&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;node_modules/typescript.d.ts&#39; does not exist.</span></span>
<span class="line"><span style="color:#24292e;">Found &#39;package.json&#39; at &#39;node_modules/typescript/package.json&#39;.</span></span>
<span class="line"><span style="color:#24292e;">&#39;package.json&#39; has &#39;types&#39; field &#39;./lib/typescript.d.ts&#39; that references &#39;node_modules/typescript/lib/typescript.d.ts&#39;.</span></span>
<span class="line"><span style="color:#24292e;">File &#39;node_modules/typescript/lib/typescript.d.ts&#39; exist - use it as a module resolution result.</span></span>
<span class="line"><span style="color:#24292e;">======== Module name &#39;typescript&#39; was successfully resolved to &#39;node_modules/typescript/lib/typescript.d.ts&#39;. ========</span></span></code></pre></div><h4 id="things-to-look-out-for" tabindex="-1">Things to look out for <a class="header-anchor" href="#things-to-look-out-for" aria-label="Permalink to &quot;Things to look out for&quot;">​</a></h4><ul><li>Name and location of the import</li></ul><blockquote><p>======== Resolving module <strong>&#39;typescript&#39;</strong> from <strong>&#39;src/app.ts&#39;</strong>. ========</p></blockquote><ul><li>The strategy the compiler is following</li></ul><blockquote><p>Module resolution kind is not specified, using <strong>&#39;NodeJs&#39;</strong>.</p></blockquote><ul><li>Loading of types from npm packages</li></ul><blockquote><p>&#39;package.json&#39; has <strong>&#39;types&#39;</strong> field &#39;./lib/typescript.d.ts&#39; that references &#39;node_modules/typescript/lib/typescript.d.ts&#39;.</p></blockquote><ul><li>Final result</li></ul><blockquote><p>======== Module name &#39;typescript&#39; was <strong>successfully resolved</strong> to &#39;node_modules/typescript/lib/typescript.d.ts&#39;. ========</p></blockquote><h2 id="using-noresolve" tabindex="-1">Using <code>--noResolve</code> <a class="header-anchor" href="#using-noresolve" aria-label="Permalink to &quot;Using \`--noResolve\`&quot;">​</a></h2><p>Normally the compiler will attempt to resolve all module imports before it starts the compilation process. Every time it successfully resolves an <code>import</code> to a file, the file is added to the set of files the compiler will process later on.</p><p>The <a href="/tsdoc-vitepress/tsconfig#noResolve"><code>noResolve</code></a> compiler options instructs the compiler not to &quot;add&quot; any files to the compilation that were not passed on the command line. It will still try to resolve the module to files, but if the file is not specified, it will not be included.</p><p>For instance:</p><h4 id="app-ts" tabindex="-1">app.ts <a class="header-anchor" href="#app-ts" aria-label="Permalink to &quot;app.ts&quot;">​</a></h4><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F286C4;">import</span><span style="color:#F6F6F4;"> </span><span style="color:#BF9EEE;">*</span><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">as</span><span style="color:#F6F6F4;"> A </span><span style="color:#F286C4;">from</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">moduleA</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">; </span><span style="color:#7B7F8B;">// OK, &#39;moduleA&#39; passed on the command-line</span></span>
<span class="line"><span style="color:#F286C4;">import</span><span style="color:#F6F6F4;"> </span><span style="color:#BF9EEE;">*</span><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">as</span><span style="color:#F6F6F4;"> B </span><span style="color:#F286C4;">from</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">moduleB</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">; </span><span style="color:#7B7F8B;">// Error TS2307: Cannot find module &#39;moduleB&#39;.</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">import</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">*</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">as</span><span style="color:#24292E;"> A </span><span style="color:#D73A49;">from</span><span style="color:#24292E;"> </span><span style="color:#032F62;">&quot;moduleA&quot;</span><span style="color:#24292E;">; </span><span style="color:#6A737D;">// OK, &#39;moduleA&#39; passed on the command-line</span></span>
<span class="line"><span style="color:#D73A49;">import</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">*</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">as</span><span style="color:#24292E;"> B </span><span style="color:#D73A49;">from</span><span style="color:#24292E;"> </span><span style="color:#032F62;">&quot;moduleB&quot;</span><span style="color:#24292E;">; </span><span style="color:#6A737D;">// Error TS2307: Cannot find module &#39;moduleB&#39;.</span></span></code></pre></div><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">tsc </span><span style="color:#E7EE98;">app.ts</span><span style="color:#F6F6F4;"> </span><span style="color:#E7EE98;">moduleA.ts</span><span style="color:#F6F6F4;"> </span><span style="color:#BF9EEE;">--noResolve</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6F42C1;">tsc</span><span style="color:#24292E;"> </span><span style="color:#032F62;">app.ts</span><span style="color:#24292E;"> </span><span style="color:#032F62;">moduleA.ts</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">--noResolve</span></span></code></pre></div><p>Compiling <code>app.ts</code> using <a href="/tsdoc-vitepress/tsconfig#noResolve"><code>noResolve</code></a> should result in:</p><ul><li>Correctly finding <code>moduleA</code> as it was passed on the command-line.</li><li>Error for not finding <code>moduleB</code> as it was not passed.</li></ul><h2 id="common-questions" tabindex="-1">Common Questions <a class="header-anchor" href="#common-questions" aria-label="Permalink to &quot;Common Questions&quot;">​</a></h2><h3 id="why-does-a-module-in-the-exclude-list-still-get-picked-up-by-the-compiler" tabindex="-1">Why does a module in the exclude list still get picked up by the compiler? <a class="header-anchor" href="#why-does-a-module-in-the-exclude-list-still-get-picked-up-by-the-compiler" aria-label="Permalink to &quot;Why does a module in the exclude list still get picked up by the compiler?&quot;">​</a></h3><p><code>tsconfig.json</code> turns a folder into a “project”. Without specifying any <code>“exclude”</code> or <code>“files”</code> entries, all files in the folder containing the <code>tsconfig.json</code> and all its sub-directories are included in your compilation. If you want to exclude some of the files use <code>“exclude”</code>, if you would rather specify all the files instead of letting the compiler look them up, use <code>“files”</code>.</p><p>That was <code>tsconfig.json</code> automatic inclusion. That does not embed module resolution as discussed above. If the compiler identified a file as a target of a module import, it will be included in the compilation regardless if it was excluded in the previous steps.</p><p>So to exclude a file from the compilation, you need to exclude it and <strong>all</strong> files that have an <code>import</code> or <code>/// &lt;reference path=&quot;...&quot; /&gt;</code> directive to it.</p>`,119),n=[a];function p(i,r,c,d,u,m){return o(),s("div",null,n)}const f=e(t,[["render",p]]);export{y as __pageData,f as default};
