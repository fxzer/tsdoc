import{_ as e,o as s,c as o,V as a}from"./chunks/framework.ced88878.js";const y=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/tsconfig-reference/project-config/Project References.md","lastUpdated":1682670826000}'),n={name:"en/tsconfig-reference/project-config/Project References.md"},t=a(`<p>Project references are a new feature in TypeScript 3.0 that allow you to structure your TypeScript programs into smaller pieces.</p><p>By doing this, you can greatly improve build times, enforce logical separation between components, and organize your code in new and better ways.</p><p>We&#39;re also introducing a new mode for <code>tsc</code>, the <code>--build</code> flag, that works hand in hand with project references to enable faster TypeScript builds.</p><h2 id="an-example-project" tabindex="-1">An Example Project <a class="header-anchor" href="#an-example-project" aria-label="Permalink to &quot;An Example Project&quot;">​</a></h2><p>Let&#39;s look at a fairly normal program and see how project references can help us better organize it. Imagine you have a project with two modules, <code>converter</code> and <code>units</code>, and a corresponding test file for each:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">/</span></span>
<span class="line"><span style="color:#f6f6f4;">├── src/</span></span>
<span class="line"><span style="color:#f6f6f4;">│   ├── converter.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">│   └── units.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">├── test/</span></span>
<span class="line"><span style="color:#f6f6f4;">│   ├── converter-tests.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">│   └── units-tests.ts</span></span>
<span class="line"><span style="color:#f6f6f4;">└── tsconfig.json</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">/</span></span>
<span class="line"><span style="color:#24292e;">├── src/</span></span>
<span class="line"><span style="color:#24292e;">│   ├── converter.ts</span></span>
<span class="line"><span style="color:#24292e;">│   └── units.ts</span></span>
<span class="line"><span style="color:#24292e;">├── test/</span></span>
<span class="line"><span style="color:#24292e;">│   ├── converter-tests.ts</span></span>
<span class="line"><span style="color:#24292e;">│   └── units-tests.ts</span></span>
<span class="line"><span style="color:#24292e;">└── tsconfig.json</span></span></code></pre></div><p>The test files import the implementation files and do some testing:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#7B7F8B;">// converter-tests.ts</span></span>
<span class="line"><span style="color:#F286C4;">import</span><span style="color:#F6F6F4;"> </span><span style="color:#BF9EEE;">*</span><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">as</span><span style="color:#F6F6F4;"> converter </span><span style="color:#F286C4;">from</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">../converter</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F6F6F4;">assert.</span><span style="color:#62E884;">areEqual</span><span style="color:#F6F6F4;">(converter.</span><span style="color:#62E884;">celsiusToFahrenheit</span><span style="color:#F6F6F4;">(</span><span style="color:#BF9EEE;">0</span><span style="color:#F6F6F4;">), </span><span style="color:#BF9EEE;">32</span><span style="color:#F6F6F4;">);</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#6A737D;">// converter-tests.ts</span></span>
<span class="line"><span style="color:#D73A49;">import</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">*</span><span style="color:#24292E;"> </span><span style="color:#D73A49;">as</span><span style="color:#24292E;"> converter </span><span style="color:#D73A49;">from</span><span style="color:#24292E;"> </span><span style="color:#032F62;">&quot;../converter&quot;</span><span style="color:#24292E;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#24292E;">assert.</span><span style="color:#6F42C1;">areEqual</span><span style="color:#24292E;">(converter.</span><span style="color:#6F42C1;">celsiusToFahrenheit</span><span style="color:#24292E;">(</span><span style="color:#005CC5;">0</span><span style="color:#24292E;">), </span><span style="color:#005CC5;">32</span><span style="color:#24292E;">);</span></span></code></pre></div><p>Previously, this structure was rather awkward to work with if you used a single tsconfig file:</p><ul><li>It was possible for the implementation files to import the test files</li><li>It wasn&#39;t possible to build <code>test</code> and <code>src</code> at the same time without having <code>src</code> appear in the output folder name, which you probably don&#39;t want</li><li>Changing just the <em>internals</em> in the implementation files required <em>typechecking</em> the tests again, even though this wouldn&#39;t ever cause new errors</li><li>Changing just the tests required typechecking the implementation again, even if nothing changed</li></ul><p>You could use multiple tsconfig files to solve <em>some</em> of those problems, but new ones would appear:</p><ul><li>There&#39;s no built-in up-to-date checking, so you end up always running <code>tsc</code> twice</li><li>Invoking <code>tsc</code> twice incurs more startup time overhead</li><li><code>tsc -w</code> can&#39;t run on multiple config files at once</li></ul><p>Project references can solve all of these problems and more.</p><h2 id="what-is-a-project-reference" tabindex="-1">What is a Project Reference? <a class="header-anchor" href="#what-is-a-project-reference" aria-label="Permalink to &quot;What is a Project Reference?&quot;">​</a></h2><p><code>tsconfig.json</code> files have a new top-level property, <a href="/tsdoc-vitepress/tsconfig#references"><code>references</code></a>. It&#39;s an array of objects that specifies projects to reference:</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">{</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">compilerOptions</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">: {</span></span>
<span class="line"><span style="color:#F6F6F4;">        </span><span style="color:#7B7F8B;">// The usual</span></span>
<span class="line"><span style="color:#F6F6F4;">    },</span></span>
<span class="line"><span style="color:#F6F6F4;">    </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">references</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">: [</span></span>
<span class="line"><span style="color:#F6F6F4;">        { </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">path</span><span style="color:#DEE492;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">../src</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;"> }</span></span>
<span class="line"><span style="color:#F6F6F4;">    ]</span></span>
<span class="line"><span style="color:#F6F6F4;">}</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">{</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#032F62;">&quot;compilerOptions&quot;</span><span style="color:#24292E;">: {</span></span>
<span class="line"><span style="color:#24292E;">        </span><span style="color:#6A737D;">// The usual</span></span>
<span class="line"><span style="color:#24292E;">    },</span></span>
<span class="line"><span style="color:#24292E;">    </span><span style="color:#032F62;">&quot;references&quot;</span><span style="color:#24292E;">: [</span></span>
<span class="line"><span style="color:#24292E;">        { </span><span style="color:#032F62;">&quot;path&quot;</span><span style="color:#24292E;">: </span><span style="color:#032F62;">&quot;../src&quot;</span><span style="color:#24292E;"> }</span></span>
<span class="line"><span style="color:#24292E;">    ]</span></span>
<span class="line"><span style="color:#24292E;">}</span></span></code></pre></div><p>The <code>path</code> property of each reference can point to a directory containing a <code>tsconfig.json</code> file, or to the config file itself (which may have any name).</p><p>When you reference a project, new things happen:</p><ul><li>Importing modules from a referenced project will instead load its <em>output</em> declaration file (<code>.d.ts</code>)</li><li>If the referenced project produces an <a href="/tsdoc-vitepress/tsconfig#outFile"><code>outFile</code></a>, the output file <code>.d.ts</code> file&#39;s declarations will be visible in this project</li><li>Build mode (see below) will automatically build the referenced project if needed</li></ul><p>By separating into multiple projects, you can greatly improve the speed of typechecking and compiling, reduce memory usage when using an editor, and improve enforcement of the logical groupings of your program.</p><h2 id="composite" tabindex="-1"><code>composite</code> <a class="header-anchor" href="#composite" aria-label="Permalink to &quot;\`composite\`&quot;">​</a></h2><p>Referenced projects must have the new <a href="/tsdoc-vitepress/tsconfig#composite"><code>composite</code></a> setting enabled. This setting is needed to ensure TypeScript can quickly determine where to find the outputs of the referenced project. Enabling the <a href="/tsdoc-vitepress/tsconfig#composite"><code>composite</code></a> flag changes a few things:</p><ul><li>The <a href="/tsdoc-vitepress/tsconfig#rootDir"><code>rootDir</code></a> setting, if not explicitly set, defaults to the directory containing the <code>tsconfig</code> file</li><li>All implementation files must be matched by an <a href="/tsdoc-vitepress/tsconfig#include"><code>include</code></a> pattern or listed in the <a href="/tsdoc-vitepress/tsconfig#files"><code>files</code></a> array. If this constraint is violated, <code>tsc</code> will inform you which files weren&#39;t specified</li><li><a href="/tsdoc-vitepress/tsconfig#declaration"><code>declaration</code></a> must be turned on</li></ul><h2 id="declarationmaps" tabindex="-1"><code>declarationMap</code>s <a class="header-anchor" href="#declarationmaps" aria-label="Permalink to &quot;\`declarationMap\`s&quot;">​</a></h2><p>We&#39;ve also added support for <a href="https://github.com/Microsoft/TypeScript/issues/14479" target="_blank" rel="noreferrer">declaration source maps</a>. If you enable <a href="/tsdoc-vitepress/tsconfig#declarationMap"><code>declarationMap</code></a>, you&#39;ll be able to use editor features like &quot;Go to Definition&quot; and Rename to transparently navigate and edit code across project boundaries in supported editors.</p><h2 id="prepend-with-outfile" tabindex="-1"><code>prepend</code> with <code>outFile</code> <a class="header-anchor" href="#prepend-with-outfile" aria-label="Permalink to &quot;\`prepend\` with \`outFile\`&quot;">​</a></h2><p>You can also enable prepending the output of a dependency using the <code>prepend</code> option in a reference:</p><div class="language-js"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">references</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">: [</span></span>
<span class="line"><span style="color:#F6F6F4;">       { </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">path</span><span style="color:#DEE492;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">../utils</span><span style="color:#DEE492;">&quot;</span><span style="color:#F6F6F4;">, </span><span style="color:#DEE492;">&quot;</span><span style="color:#E7EE98;">prepend</span><span style="color:#DEE492;">&quot;</span><span style="color:#F286C4;">:</span><span style="color:#F6F6F4;"> </span><span style="color:#BF9EEE;">true</span><span style="color:#F6F6F4;"> }</span></span>
<span class="line"><span style="color:#F6F6F4;">   ]</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#032F62;">&quot;references&quot;</span><span style="color:#24292E;">: [</span></span>
<span class="line"><span style="color:#24292E;">       { </span><span style="color:#032F62;">&quot;path&quot;</span><span style="color:#24292E;">: </span><span style="color:#032F62;">&quot;../utils&quot;</span><span style="color:#24292E;">, </span><span style="color:#032F62;">&quot;prepend&quot;</span><span style="color:#24292E;">: </span><span style="color:#005CC5;">true</span><span style="color:#24292E;"> }</span></span>
<span class="line"><span style="color:#24292E;">   ]</span></span></code></pre></div><p>Prepending a project will include the project&#39;s output above the output of the current project. All output files (<code>.js</code>, <code>.d.ts</code>, <code>.js.map</code>, <code>.d.ts.map</code>) will be emitted correctly.</p><p><code>tsc</code> will only ever use existing files on disk to do this process, so it&#39;s possible to create a project where a correct output file can&#39;t be generated because some project&#39;s output would be present more than once in the resulting file. For example:</p><div class="language-txt"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#f6f6f4;">A</span></span>
<span class="line"><span style="color:#f6f6f4;">  ^ ^</span></span>
<span class="line"><span style="color:#f6f6f4;"> /   \\</span></span>
<span class="line"><span style="color:#f6f6f4;">B     C</span></span>
<span class="line"><span style="color:#f6f6f4;"> ^   ^</span></span>
<span class="line"><span style="color:#f6f6f4;">  \\ /</span></span>
<span class="line"><span style="color:#f6f6f4;">   D</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292e;">A</span></span>
<span class="line"><span style="color:#24292e;">  ^ ^</span></span>
<span class="line"><span style="color:#24292e;"> /   \\</span></span>
<span class="line"><span style="color:#24292e;">B     C</span></span>
<span class="line"><span style="color:#24292e;"> ^   ^</span></span>
<span class="line"><span style="color:#24292e;">  \\ /</span></span>
<span class="line"><span style="color:#24292e;">   D</span></span></code></pre></div><p>It&#39;s important in this situation to not prepend at each reference, because you&#39;ll end up with two copies of <code>A</code> in the output of <code>D</code> - this can lead to unexpected results.</p><h2 id="caveats-for-project-references" tabindex="-1">Caveats for Project References <a class="header-anchor" href="#caveats-for-project-references" aria-label="Permalink to &quot;Caveats for Project References&quot;">​</a></h2><p>Project references have a few trade-offs you should be aware of.</p><p>Because dependent projects make use of <code>.d.ts</code> files that are built from their dependencies, you&#39;ll either have to check in certain build outputs <em>or</em> build a project after cloning it before you can navigate the project in an editor without seeing spurious errors.</p><p>When using VS Code (since TS 3.7) we have a behind-the-scenes in-memory <code>.d.ts</code> generation process that should be able to mitigate this, but it has some perf implications. For very large composite projects you might want to disable this using <a href="/tsdoc-vitepress/tsconfig#disableSourceOfProjectReferenceRedirect">disableSourceOfProjectReferenceRedirect option</a>.</p><p>Additionally, to preserve compatibility with existing build workflows, <code>tsc</code> will <em>not</em> automatically build dependencies unless invoked with the <code>--build</code> switch. Let&#39;s learn more about <code>--build</code>.</p><h2 id="build-mode-for-typescript" tabindex="-1">Build Mode for TypeScript <a class="header-anchor" href="#build-mode-for-typescript" aria-label="Permalink to &quot;Build Mode for TypeScript&quot;">​</a></h2><p>A long-awaited feature is smart incremental builds for TypeScript projects. In 3.0 you can use the <code>--build</code> flag with <code>tsc</code>. This is effectively a new entry point for <code>tsc</code> that behaves more like a build orchestrator than a simple compiler.</p><p>Running <code>tsc --build</code> (<code>tsc -b</code> for short) will do the following:</p><ul><li>Find all referenced projects</li><li>Detect if they are up-to-date</li><li>Build out-of-date projects in the correct order</li></ul><p>You can provide <code>tsc -b</code> with multiple config file paths (e.g. <code>tsc -b src test</code>). Just like <code>tsc -p</code>, specifying the config file name itself is unnecessary if it&#39;s named <code>tsconfig.json</code>.</p><h2 id="tsc-b-commandline" tabindex="-1"><code>tsc -b</code> Commandline <a class="header-anchor" href="#tsc-b-commandline" aria-label="Permalink to &quot;\`tsc -b\` Commandline&quot;">​</a></h2><p>You can specify any number of config files:</p><div class="language-shell"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F286C4;">&gt;</span><span style="color:#F6F6F4;"> tsc -b                            </span><span style="color:#7B7F8B;"># Use the tsconfig.json in the current directory</span></span>
<span class="line"><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">&gt;</span><span style="color:#F6F6F4;"> tsc </span><span style="color:#BF9EEE;">-b</span><span style="color:#F6F6F4;"> </span><span style="color:#E7EE98;">src</span><span style="color:#F6F6F4;">                        </span><span style="color:#7B7F8B;"># Use src/tsconfig.json</span></span>
<span class="line"><span style="color:#F6F6F4;"> </span><span style="color:#F286C4;">&gt;</span><span style="color:#F6F6F4;"> tsc </span><span style="color:#BF9EEE;">-b</span><span style="color:#F6F6F4;"> </span><span style="color:#E7EE98;">foo/prd.tsconfig.json</span><span style="color:#F6F6F4;"> </span><span style="color:#E7EE98;">bar</span><span style="color:#F6F6F4;">  </span><span style="color:#7B7F8B;"># Use foo/prd.tsconfig.json and bar/tsconfig.json</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#D73A49;">&gt;</span><span style="color:#24292E;"> tsc -b                            </span><span style="color:#6A737D;"># Use the tsconfig.json in the current directory</span></span>
<span class="line"><span style="color:#24292E;"> </span><span style="color:#D73A49;">&gt;</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">tsc</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">-b</span><span style="color:#24292E;"> </span><span style="color:#032F62;">src</span><span style="color:#24292E;">                        </span><span style="color:#6A737D;"># Use src/tsconfig.json</span></span>
<span class="line"><span style="color:#24292E;"> </span><span style="color:#D73A49;">&gt;</span><span style="color:#24292E;"> </span><span style="color:#6F42C1;">tsc</span><span style="color:#24292E;"> </span><span style="color:#005CC5;">-b</span><span style="color:#24292E;"> </span><span style="color:#032F62;">foo/prd.tsconfig.json</span><span style="color:#24292E;"> </span><span style="color:#032F62;">bar</span><span style="color:#24292E;">  </span><span style="color:#6A737D;"># Use foo/prd.tsconfig.json and bar/tsconfig.json</span></span></code></pre></div><p>Don&#39;t worry about ordering the files you pass on the commandline - <code>tsc</code> will re-order them if needed so that dependencies are always built first.</p><p>There are also some flags specific to <code>tsc -b</code>:</p><ul><li><a href="/tsdoc-vitepress/tsconfig#verbose"><code>--verbose</code></a>: Prints out verbose logging to explain what&#39;s going on (may be combined with any other flag)</li><li><code>--dry</code>: Shows what would be done but doesn&#39;t actually build anything</li><li><code>--clean</code>: Deletes the outputs of the specified projects (may be combined with <code>--dry</code>)</li><li><a href="/tsdoc-vitepress/tsconfig#force"><code>--force</code></a>: Act as if all projects are out of date</li><li><code>--watch</code>: Watch mode (may not be combined with any flag except <a href="/tsdoc-vitepress/tsconfig#verbose"><code>--verbose</code></a>)</li></ul><h2 id="caveats" tabindex="-1">Caveats <a class="header-anchor" href="#caveats" aria-label="Permalink to &quot;Caveats&quot;">​</a></h2><p>Normally, <code>tsc</code> will produce outputs (<code>.js</code> and <code>.d.ts</code>) in the presence of syntax or type errors, unless <a href="/tsdoc-vitepress/tsconfig#noEmitOnError"><code>noEmitOnError</code></a> is on. Doing this in an incremental build system would be very bad - if one of your out-of-date dependencies had a new error, you&#39;d only see it <em>once</em> because a subsequent build would skip building the now up-to-date project. For this reason, <code>tsc -b</code> effectively acts as if <a href="/tsdoc-vitepress/tsconfig#noEmitOnError"><code>noEmitOnError</code></a> is enabled for all projects.</p><p>If you check in any build outputs (<code>.js</code>, <code>.d.ts</code>, <code>.d.ts.map</code>, etc.), you may need to run a <a href="/tsdoc-vitepress/tsconfig#force"><code>--force</code></a> build after certain source control operations depending on whether your source control tool preserves timestamps between the local copy and the remote copy.</p><h2 id="msbuild" tabindex="-1">MSBuild <a class="header-anchor" href="#msbuild" aria-label="Permalink to &quot;MSBuild&quot;">​</a></h2><p>If you have an msbuild project, you can enable build mode by adding</p><div class="language-xml"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki dracula-soft vp-code-dark"><code><span class="line"><span style="color:#F6F6F4;">&lt;</span><span style="color:#F286C4;">TypeScriptBuildMode</span><span style="color:#F6F6F4;">&gt;true&lt;/</span><span style="color:#F286C4;">TypeScriptBuildMode</span><span style="color:#F6F6F4;">&gt;</span></span></code></pre><pre class="shiki github-light vp-code-light"><code><span class="line"><span style="color:#24292E;">&lt;</span><span style="color:#22863A;">TypeScriptBuildMode</span><span style="color:#24292E;">&gt;true&lt;/</span><span style="color:#22863A;">TypeScriptBuildMode</span><span style="color:#24292E;">&gt;</span></span></code></pre></div><p>to your proj file. This will enable automatic incremental build as well as cleaning.</p><p>Note that as with <code>tsconfig.json</code> / <code>-p</code>, existing TypeScript project properties will not be respected - all settings should be managed using your tsconfig file.</p><p>Some teams have set up msbuild-based workflows wherein tsconfig files have the same <em>implicit</em> graph ordering as the managed projects they are paired with. If your solution is like this, you can continue to use <code>msbuild</code> with <code>tsc -p</code> along with project references; these are fully interoperable.</p><h2 id="guidance" tabindex="-1">Guidance <a class="header-anchor" href="#guidance" aria-label="Permalink to &quot;Guidance&quot;">​</a></h2><h2 id="overall-structure" tabindex="-1">Overall Structure <a class="header-anchor" href="#overall-structure" aria-label="Permalink to &quot;Overall Structure&quot;">​</a></h2><p>With more <code>tsconfig.json</code> files, you&#39;ll usually want to use <a href="/tsdoc-vitepress/docs/handbook/tsconfig-json.html">Configuration file inheritance</a> to centralize your common compiler options. This way you can change a setting in one file rather than having to edit multiple files.</p><p>Another good practice is to have a &quot;solution&quot; <code>tsconfig.json</code> file that simply has <a href="/tsdoc-vitepress/tsconfig#references"><code>references</code></a> to all of your leaf-node projects and sets <a href="/tsdoc-vitepress/tsconfig#files"><code>files</code></a> to an empty array (otherwise the solution file will cause double compilation of files). Note that starting with 3.0, it is no longer an error to have an empty <a href="/tsdoc-vitepress/tsconfig#files"><code>files</code></a> array if you have at least one <code>reference</code> in a <code>tsconfig.json</code> file.</p><p>This presents a simple entry point; e.g. in the TypeScript repo we simply run <code>tsc -b src</code> to build all endpoints because we list all the subprojects in <code>src/tsconfig.json</code></p><p>You can see these patterns in the TypeScript repo - see <code>src/tsconfig_base.json</code>, <code>src/tsconfig.json</code>, and <code>src/tsc/tsconfig.json</code> as key examples.</p><h2 id="structuring-for-relative-modules" tabindex="-1">Structuring for relative modules <a class="header-anchor" href="#structuring-for-relative-modules" aria-label="Permalink to &quot;Structuring for relative modules&quot;">​</a></h2><p>In general, not much is needed to transition a repo using relative modules. Simply place a <code>tsconfig.json</code> file in each subdirectory of a given parent folder, and add <code>reference</code>s to these config files to match the intended layering of the program. You will need to either set the <a href="/tsdoc-vitepress/tsconfig#outDir"><code>outDir</code></a> to an explicit subfolder of the output folder, or set the <a href="/tsdoc-vitepress/tsconfig#rootDir"><code>rootDir</code></a> to the common root of all project folders.</p><h2 id="structuring-for-outfiles" tabindex="-1">Structuring for outFiles <a class="header-anchor" href="#structuring-for-outfiles" aria-label="Permalink to &quot;Structuring for outFiles&quot;">​</a></h2><p>Layout for compilations using <a href="/tsdoc-vitepress/tsconfig#outFile"><code>outFile</code></a> is more flexible because relative paths don&#39;t matter as much. One thing to keep in mind is that you&#39;ll generally want to not use <code>prepend</code> until the &quot;last&quot; project - this will improve build times and reduce the amount of I/O needed in any given build. The TypeScript repo itself is a good reference here - we have some &quot;library&quot; projects and some &quot;endpoint&quot; projects; &quot;endpoint&quot; projects are kept as small as possible and pull in only the libraries they need.</p>`,67),l=[t];function c(p,r,i,d,u,f){return s(),o("div",null,l)}const g=e(n,[["render",c]]);export{y as __pageData,g as default};
